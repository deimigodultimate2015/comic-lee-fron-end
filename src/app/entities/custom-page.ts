export class CustomPage {
    id: number;
    index: string;
    name: string;

    constructor(id: number, name: string, index: string) {
        this.name = name;
        this.id = id;
        this.index = index;
    }
}
