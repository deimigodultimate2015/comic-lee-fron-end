export class CustomPage {
    id: number;
    index: string;
    name: string;

    constructor(id: number, cdn: string, index: string) {
        this.name = name;
        this.id = id;
        this.index = index;
    }
}
